# API base URL (origin only, no path). Leave empty for local dev (uses Vite proxy /api -> backend).
# Production (e.g. Vercel): set to your API host so portal and contract-signing use it.
# VITE_API_BASE=https://ponot.sigmasolusiservis.com
